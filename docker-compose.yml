services:
  agentflow:
    build: .
    ports:
      - "9600:9600"
    env_file:
      - .env
    environment:
      - AGENTFLOW_HOST=0.0.0.0
    restart: unless-stopped
    volumes:
      - .agentflow:/app/.agentflow
